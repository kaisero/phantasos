"""Runtime hooks for the {{ package }} CLI.

This file is generated once and then hand-owned. Re-running the build will NOT
overwrite it.

Define any of the hook functions below to customise behaviour. Each hook is
called by the generated command layer at the indicated moment.

Example hooks (uncomment and implement as needed):

    def before_request(ctx: dict) -> dict:
        \"\"\"Mutate the SDK call kwargs before they are dispatched.\"\"\"
        return ctx

    def after_response(result: object) -> object:
        \"\"\"Transform or log the SDK response before it is rendered.\"\"\"
        return result
"""
