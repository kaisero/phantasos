# {{ distribution }}

{{ description }}

> Generated by [phantasos](https://github.com/kaisero/phantasos). Do not hand-edit — changes
> are made upstream in the phantasos product definition and regenerated.

## Install

```bash
pip install {{ distribution }}
```

## Usage

```python
from {{ package }}.extras.facade import Client

client = Client.from_env()

# List posture checks (offset/limit pagination).
checks = list(client.paginate(client.posture_checks.list_posture_checks))
```

Authentication uses SCM OAuth2 client-credentials. Set `CLIENT_ID`, `CLIENT_SECRET`,
and `SCOPE`; override the host with `POSTURE_BASE_URL` if needed.

See [{{ repo_url }}]({{ repo_url }}) for full documentation.
