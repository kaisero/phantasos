# Secrets — never commit live tenant credentials
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
.ruff_cache/

# Claude-authored reports (kept on disk, not part of the SDK)
GENERATION_NOTES.md
