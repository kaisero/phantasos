# Secrets — never commit live tenant credentials
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
.ruff_cache/

# Build artifacts
dist/
build/
*.egg-info/

# sdkgen per-build preprocess scratch
.sdkgen/

# Claude-authored reports (kept on disk, not part of the SDK)
GENERATION_NOTES.md

# macOS
.DS_Store

# Vendored generator jar (fetched by make)
.tools/

# stray sed temp files
sed??????
